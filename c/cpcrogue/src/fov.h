/****************************************************************************
 *    Line
 *
 * Traces a line from x1,y1 to x2,y2 using Bresenham's algorithm and
 * returns the coordinates as an array coords.
 */
void TraceLine (u8 x1, u8 y1, register u8 x2, register u8 y2);//, u8 *coords);